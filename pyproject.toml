[project]
name = "manifest-featurizer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "spdx-license-list>=3.20",
    "license-expression>=30.0",
    "pyyaml>=6.0",
    "tqdm>=4.0",
]
